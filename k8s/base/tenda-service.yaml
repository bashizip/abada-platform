apiVersion: v1
kind: Service
metadata:
  name: abada-tenda
  namespace: abada
  labels:
    app.kubernetes.io/name: abada-tenda
    app.kubernetes.io/component: frontend
    app.kubernetes.io/part-of: abada-platform
spec:
  type: ClusterIP
  selector:
    app.kubernetes.io/name: abada-tenda
  ports:
    - name: http
      port: 5602
      targetPort: http
      protocol: TCP
