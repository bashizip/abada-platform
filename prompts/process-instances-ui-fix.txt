this is not working at all. for the 2 places where we see a running process we should have the ssame behabiour in the canvas: show clearly the running node and the path taken by the workflow to arrive at that node. showing a glowing dot at the right node an animating a path between two other nodes is so dumb !.
We should animate the path between the paths just like in the dry run test where it really makes sense : we have 2 options: 1. animmate the full historical path taken by the process instance until the active node that have the glowing dot; 2. animate only the path between the active node and the all adjecent ones ( last path and all the possible next ). Let me know if you need futher clarification dont implement dumbly
