# Abada 1.1 Product & Architecture Execution Plan

- Status: Approved for phased execution — Part 1 (studio surfaces) and
  Phase 1 (native APL persistence + polymorphic loader) delivered on
  2026-08-08; Phases 2–4 pending.
- Date: 2026-08-07
- Target: Post-BPMN transpilation engineering & Studio UI roadmap
- Related: `adr/ADR-002-native-decision-tables-deterministic-wall.md`,
  `adr/ADR-003-insight-loop-engine-otel-apl.md`,
  `development/studio-app-spec.md`, `development/roadmap-to-1.1.0-rc.md`,
  `reference/apl-specification.md`, `operations/observability.md`

## Context

Abada is transitioning from BPMN-compiled execution to a fully native,
self-optimizing APL (Abada Process Language) runtime with enterprise
governance. This plan covers the product and architecture execution for that
transition: studio UI/UX surfaces first, then the post-transpilation engine
core.

Execution proceeds phase by phase. Each phase must preserve the BPMN runtime
contracts and verification expectations recorded in `AGENTS.md` and the 1.1
roadmap before the next phase starts.

---

## Part 1 — Studio UI/UX

### Task 1.1: Format badge & file metadata indicator

- [ ] Add visual format pills in the header and process list (`.apl.yaml` vs
  `.bpmn`). *Partial: process list shows format pills (Sidebar via
  `getFileFormatLabel`); the main header intentionally keeps a clean file
  identifier without pill noise.*
- [x] Display runtime status tags on open files (`[APL Native]` vs
  `[BPMN Imported]`) — Sidebar list pills and Process Details modal tag.
- [x] Expose the active language spec version (`abada.io/v1`) in the Process
  Details modal metadata row (`LANGUAGE_VERSION_ABADA_IO_V1`).

### Task 1.2: Native DMN rule inspector widget

- [x] Update the right-side Inspector panel for `decision-table` / `DMN` nodes
  (`DmnRuleInspector` mounted in `PropertiesInspector`).
- [x] Build a matrix editor component supporting:
  - Input variable mappings (`name` + `${...}` expressions).
  - Ordered rules with `when` conditions and typed `then` outputs
    (`string` | `number` | `boolean`). *Displayed as a normal table preview.*
  - Hit Policy selector narrowed to `FIRST | UNIQUE | COLLECT`.
  - Explicit `otherwise` fallback row toggle (turns the row into the else
    branch).
- [x] Bidirectional sync between the matrix UI and canonical APL YAML
  (`normalizeTableInputs` / `resolveRuleOutcome` in `lib/apl/parser.ts`).

### Task 1.3: AI optimization visual diff layer ("AI Diff")

- [x] Create a visual review mode for proposed workflow modifications.
  *Implemented as a full-focus review dialog (`AIDiffModal`); the canvas
  stays untouched while a proposal is reviewed (planned overlay mode
  deferred).*
- [x] Render added nodes/edges in green highlight and removed or modified
  paths in red/amber (ADDED/MODIFIED/REMOVED kind markers, graph + YAML
  tabs).
- [x] Add annotation rendering for `#` comments generated by the optimization
  engine (per-node tooltips and the YAML tab's annotated diff).

---

## Part 2 — Post-Transpilation Core

### Phase 1: Native APL persistence (engine core)

- [x] **Database migration:** add a `schema_type` column (`BPMN_XML` vs
  `APL_NATIVE`, NOT NULL default, CHECK-constrained) to
  `process_definitions` — V10 Flyway migration; every published schema
  version v1–v9 upgrades cleanly under PostgreSQL. Immutable per-version
  definition rows were already the model.
- [x] **Runtime graph compiler:** `AplParser` compiles raw `abada.io/v1` YAML
  directly into the canonical executable `ParsedProcessDefinition` graph —
  no XML round-trip, no intermediate document (webhook / end / agent /
  engine-task / decision-table / approval-gate / condition nodes).
- [x] **Polymorphic definition loader:** `AbadaEngine` sniffs the deployment
  stream (APL schema marker vs XML) and dispatches to the APL or BPMN
  compiler; persisted definitions are recompiled through the same schema
  dispatch on restart, alongside legacy BPMN XML.

### Phase 2: Continuous optimization engine (the Insight Loop)

- [ ] **OpenTelemetry trace analyzer:** build an asynchronous worker to
  process engine OTel spans, flagging agent nodes with high failure rates,
  token latency, or fallback loops.
- [ ] **Automated APL PR generator:** implement an LLM worker that drafts
  optimized APL YAML patches, embedding explanatory `#` comments directly
  above updated nodes or DMN rules.

### Phase 3: Governance & multi-human approval workflows

- [ ] **Governance engine:** implement serial and parallel multi-stakeholder
  approval gates (e.g., Tech Lead + Compliance Officer sign-offs) before an
  AI-proposed APL patch goes live.
- [ ] **Versioning & rollback:** implement an atomic, one-click rollback
  mechanism to prior APL definition revisions in PostgreSQL.

### Phase 4: Studio live simulation & token visualizer

- [ ] **Canvas token traversal:** animate real-time token movement across
  canvas nodes during execution runs in the Run panel.
- [ ] **Variable state inspector:** render real-time top-level instance
  variable mutations after each node evaluation (probabilistic agent outputs
  vs deterministic in-transaction DMN outputs).

---

## Execution order

1. Part 1 Studio surfaces (format badges, DMN inspector, AI diff).
2. Phase 1 native APL persistence and loader.
3. Phase 2 Insight Loop with the ADR-003 telemetry pipeline.
4. Phase 3 governance gates and rollback.
5. Phase 4 live simulation and token visualizer.

Each completed phase requires the related contract documentation updates and
tests described in `AGENTS.md` verification expectations.